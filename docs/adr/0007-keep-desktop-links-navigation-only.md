# Keep desktop links navigation-only

Craft Hub accepts versioned Desktop Links for opening the Workbench or resolving one Project Reference. A Desktop Link never carries a command, grants Project Trust, clones a repository, or chooses among multiple local matches. Project resolution uses the normalized Git `origin` plus an optional repository-relative subdirectory, and registering an unresolved Project remains an explicit user action.
