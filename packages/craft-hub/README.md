# craft-hub

The local runtime and CLI for [Craft Hub](https://github.com/YunYouJun/craft-hub).

This package is an early alpha. Run the current prerelease without installing it globally:

```bash
pnpm dlx craft-hub@next app .
```

Projects are untrusted by default. Inspect discovered capabilities before granting trust or running commands.
